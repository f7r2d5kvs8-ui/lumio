# Persian alternate voice

Record the complete second-person Persian voice set into this folder with
`tools/persian-audio-recorder.html`. It intentionally uses the same deterministic
filenames as the primary `fa` folder, while the separate directory preserves
both voices for a later in-app voice selector.
